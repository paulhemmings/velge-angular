angular
    .module('DeveloperModule')
    .controller('DeveloperController', ['$scope', function($scope) {

      function initialize() {
        // set up the controller
      }
      initialize();

    }]);
